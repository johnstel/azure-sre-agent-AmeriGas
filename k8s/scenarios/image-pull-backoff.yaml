# =============================================================================
# SCENARIO 3: ImagePullBackOff - Failed Order Service Deployment
# Domain: Shared (Bulk Tank delivery orders & Cylinder Exchange restock orders)
# =============================================================================
# The order-service deployment fails after a botched container image
# release, referencing a non-existent image tag.
#
# SRE Agent can diagnose: Image pull failure, suggest checking image name,
# registry credentials, or network connectivity.
#
# HOW TO BREAK:
# kubectl apply -f k8s/scenarios/image-pull-backoff.yaml
#
# HOW TO FIX:
# kubectl apply -f k8s/base/application.yaml
# =============================================================================
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: propane
  labels:
    app: order-service
    scenario: image-pull-backoff
    sre-demo: breakable
spec:
  replicas: 2
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      nodeSelector:
        nodepool-type: user
      containers:
        - name: order-service
          # BREAKING CHANGE: This image doesn't exist
          image: ghcr.io/azure-samples/aks-store-demo/makeline-service:nonexistent-tag-v999
          imagePullPolicy: Always
          ports:
            - containerPort: 3001
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "100m"
